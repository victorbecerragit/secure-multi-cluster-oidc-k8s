# Helm Charts

Security and platform-related Helm charts used across clusters.

## Charts

*   `oidc-proxy/`: (Planned) Proxy for applications that don't support native OIDC.
*   `network-policy-base/`: (Planned) Standard network policies for new namespaces.

## Usage

```bash
helm template .
```
